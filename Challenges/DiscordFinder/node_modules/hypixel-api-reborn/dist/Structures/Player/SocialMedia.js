class SocialMedia {
    id;
    link;
    constructor(data) {
        this.id = data.id;
        this.link = data.link;
    }
}
export function parseSocialMedia(data) {
    return Object.keys(data).map((media) => new SocialMedia({ id: media, link: data[media] }));
}
export default SocialMedia;
//# sourceMappingURL=SocialMedia.js.map