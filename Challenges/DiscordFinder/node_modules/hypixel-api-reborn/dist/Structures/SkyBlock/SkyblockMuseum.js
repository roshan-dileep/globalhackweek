import SkyblockMuseumItem from './SkyblockMuseumItem.js';
import { decode } from '../../Utils/SkyblockUtils.js';
class SkyblockMuseum {
    raw;
    value;
    appraisal;
    getItems;
    getSpecial;
    constructor(data) {
        this.raw = data.m.members?.[data.uuid] ?? {};
        this.value = data.value;
        this.appraisal = data.appraisal;
        this.getItems = async () => {
            const items = [];
            for (const item of Object.keys(data?.m?.members[data?.uuid]?.items) || {}) {
                items.push(new SkyblockMuseumItem(item, data?.m?.members[data?.uuid]?.items[item], await decode(data?.m?.members[data?.uuid]?.items[item]?.items?.data)));
            }
            return items;
        };
        this.getSpecial = async () => {
            const items = [];
            for (const item of Object.keys(data?.m?.members[data?.uuid]?.special || {})) {
                items.push(new SkyblockMuseumItem(item, data?.m?.members[data?.uuid]?.special[item], await decode(data?.m?.members[data?.uuid]?.special[item]?.items?.data)));
            }
            return items;
        };
    }
}
export default SkyblockMuseum;
//# sourceMappingURL=SkyblockMuseum.js.map