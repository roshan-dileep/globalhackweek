import SkyWarsKit from './SkyWarsKit.js';
class SkyWarsKits {
    kits;
    constructor(kits) {
        this.kits = kits.map((kit) => new SkyWarsKit(kit)).filter((kit) => kit?.isKit);
    }
    get(gameMode = '', type = '') {
        return this.kits.filter((kit) => kit?.gameMode?.startsWith(gameMode) && kit?.kitType.startsWith(type));
    }
}
export default SkyWarsKits;
//# sourceMappingURL=SkyWarsKits.js.map